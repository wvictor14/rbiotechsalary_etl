version: 2
sources:
  - name: survey_results

    # override profiles.yml to point to project wide sources folder
    catalog: rbiotechsalary 
    schema: sources        
    tables:
      - name: src_responses